# PRD：AI 冰箱剩菜盲盒小程序

> ⚠️ 实现说明：本项目已按用户决定改为**网页版（H5）**，本 PRD 保留为原始需求存档；当前实现、部署与验收以 `README.md` 为准。

> 项目启动指令：AI 冰箱剩菜盲盒小程序

## 【角色设定】
你现在是一位资深的微信小程序全栈工程师兼产品经理。你的任务是根据以下产品需求文档（PRD），使用 "Vibe Coding" 模式，从零到一搭建一个名为 **"冰箱剩菜盲盒"** 的微信小程序。请严格按照以下架构和规范进行代码生成。

## 一、产品概述
这是一个主打趣味与实用结合的微信小程序。用户输入冰箱里现有的零散食材，AI 会生成一份带有 **"脱口秀幽默风格"** 的创意菜谱、搞笑菜名及摆盘建议。核心亮点是 **"一键生成朋友圈打卡海报"**，自带社交裂变属性。

## 二、技术架构选型
- **前端框架**：微信小程序原生开发（WXML + WXSS + JS）。
- **后端服务**：腾讯云开发 CloudBase（免服务器，Serverless 架构）。
- **AI 接入**：使用云开发 AI 能力，通过 Node.js 云函数调用大模型（如混元或 DeepSeek）。
- **数据存储**：CloudBase 云数据库（MongoDB），用于存储用户生成的菜谱记录。
- **UI 风格**：复古像素风 / 赛博朋克风，色彩明快，按钮带有微动效。

## 三、核心功能模块与页面拆解

### 1. 首页 (pages/index/index)
- 视觉：顶部大标题 "冰箱剩菜盲盒"，中心是一个醒目的 "摇一摇" 或 "开启魔法" 按钮。
- 交互：点击按钮进入输入页。底部展示 "黑暗料理红黑榜" 的横向滚动列表（展示历史用户的搞笑评价）。

### 2. 食材输入与生成页 (pages/create/create)
- 输入区：一个支持多行输入的文本框（Placeholder: "例如：半个洋葱、两包泡面、一瓶可乐"），支持语音输入（调用微信 wx.getRecorderManager）。
- 生成按钮：点击后触发 Loading 动画（如：一个冒泡的锅）。
- 结果展示区：
  - 【菜名】：大字号，带霓虹灯特效。
  - 【做法】：3-4 步极简做法，使用卡片式布局。
  - 【摆盘建议】：斜体字，带引号。
  - 【主厨警告】：红色高亮提示。

### 3. 海报生成页 (pages/poster/poster)
- 功能：接收上一页传来的菜谱数据，使用 wx.canvasToTempFilePath 将排版好的内容绘制为图片。
- 交互：提供 "保存到相册" 和 "分享给好友" 按钮。

## 四、数据库设计 (CloudBase Collections)
集合名：`recipes`
- `_id`: 自动生成
- `ingredients`: String（用户输入的食材）
- `recipe_data`: Object（AI 返回的 JSON 结构，包含 name, steps, plating, warning）
- `user_rating`: String（"真香" / "已进医院"）
- `create_time`: Date

## 五、AI 提示词工程 (System Prompt)
在云函数中调用大模型时，必须严格注入以下 System Prompt：

> 你是一位拥有米其林三星实力，但性格幽默、热爱互联网冲浪的"深夜食堂主厨"。用户会给你几种冰箱里快过期的奇葩食材，你需要将它们组合成一道菜。请严格以 JSON 格式输出，不要包含任何多余的解释文本。JSON 结构如下：
> `{"name": "包含谐音梗或网络热词的菜名，不超过10个字","steps": ["步骤1（脱口秀语气）", "步骤2", "步骤3"],"plating": "极其夸张或搞笑的摆盘建议","warning": "一句话提醒这道菜的风险或注意事项"}`

## 六、开发执行步骤（Agent 任务清单）
请按照以下顺序逐步执行，每完成一步请确认：
1. 环境初始化：生成小程序的 app.json 路由配置和全局样式 app.wxss。
2. 云函数开发：编写 generateRecipe 云函数，实现大模型 API 调用与 JSON 格式校验（防止 AI 输出非标准 JSON 导致前端崩溃）。
3. 前端页面构建：依次生成首页、输入页、海报页的 WXML、WXSS 和 JS 文件。
4. 海报绘制逻辑：实现 Canvas 绘制逻辑，确保多端兼容。
5. 联调与测试：打通前端到云函数再到 AI 的完整链路。

---

## 备注
- 云函数中调用 AI 使用 CloudBase TokenHub 托管模型（`@cloudbase/node-sdk` 的 `app.ai()`），默认模型 `hy3`（腾讯混元，内置免费国产 AI），可在云开发控制台启用/切换。
- 语音输入优先使用「微信同声传译」插件（WechatSI）做语音转文字，未配置插件时回退为原生录音。
